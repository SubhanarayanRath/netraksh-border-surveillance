.git
.env
*.db
*.db-journal
*.sqlite
*.sqlite3
frontend/node_modules
frontend/dist
# The runtime image never copies these in (Dockerfile only COPYs backend/,
# shared/, and the built frontend) — excluded from the build context too,
# not just unused, so the context upload itself is fast.
edge/
demo/
scripts/
tests/
docs/
certs/
yolov8n.pt
.pytest_cache
__pycache__
*.pyc
.claude
